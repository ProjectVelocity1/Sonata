# we're working on it!
the base fuctions working tho

How to run (for now)
```sh
python -m venv sonata-venv
source sonata-venv/bin/activate
pip install requests pygame pyyaml
```
Then run program
```sh
python /path/to/Sonata/main.py
```
